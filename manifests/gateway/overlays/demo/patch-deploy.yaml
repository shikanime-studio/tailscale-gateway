apiVersion: apps/v1
kind: Deployment
metadata:
  name: tailscale-gateway-controller
  namespace: tailscale-system
spec:
  template:
    spec:
      containers:
        - name: controller
          env: []
