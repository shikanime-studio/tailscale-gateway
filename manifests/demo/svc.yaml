apiVersion: v1
kind: Service
metadata:
  name: demo
  namespace: tailscale-gateway-demo
spec:
  selector:
    app.kubernetes.io/name: demo
    app.kubernetes.io/instance: demo
  ports:
    - port: 80
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: demo-api
  namespace: tailscale-gateway-demo
  labels:
    app: demo-api
spec:
  selector:
    app: demo-api
  ports:
    - name: http
      port: 80
      targetPort: http
